# Playground

Type `/` on the empty lines below and try the menu.

Fresh line to play on:



Mid-sentence trigger — type after this text: 

These must NOT open the menu:

- https://example.com/path/to/page
- word/inside
- /usr/local/bin (only the first slash counts — it opens the menu at line start!)

## Filter drills

- `/wa` → Warning callout
- `/h2` → Heading 2
- `/tab` → Table
- `/tldr` → Abstract callout (via alias)
