# Wrap tests

Select a line (or several) below, then type `/` and pick a block — the
selection should be wrapped, never lost.

Single line to wrap into a quote or callout:

Mira Holt keeps her field notes short.

Multiple lines for lists and callouts:

First observation from the greenhouse.
Second observation, one day later.
Third observation, after the rain.

Code-ish lines for the fenced wrap:

const habitat = "greenhouse";
let humidity = 0.62;

Inline targets (wrap into link, comment, math):

Fern Atlas
